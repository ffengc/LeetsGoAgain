

#include <iostream>
#include <set>
#include <vector>

using namespace std;


int main() {
    std::vector<std::set<int>> small_board_sets(9, std::set<int>());
    std::vector<std::set<int>> row_sets(9, std::set<int>());
}